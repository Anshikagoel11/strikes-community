services:
    zookeeper:
        image: confluentinc/cp-zookeeper:latest
        container_name: zookeeper
        environment:
            ZOOKEEPER_CLIENT_PORT: 2181
            ZOOKEEPER_TICK_TIME: 2000
        ports:
            - "2181:2181"
        volumes:
            - zookeeper-data:/var/lib/zookeeper/data
            - zookeeper-log:/var/lib/zookeeper/log
    kafka:
        image: confluentinc/cp-kafka:7.5.0
        container_name: kafka
        depends_on:
            - zookeeper
        ports:
            - "9092:9092"
        environment:
            KAFKA_BROKER_ID: 1
            KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
            KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
            KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
            KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
            KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
        volumes:
            - kafka:/var/lib/kafka/data

    kafka-ui:
        image: provectuslabs/kafka-ui:latest
        container_name: kafka-ui
        ports:
            - "8000:8080"
        environment:
            KAFKA_CLUSTERS_0_NAME: "local"
            KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: "kafka:29092"

    redis:
        image: redis:alpine
        container_name: redis-session-manager
        ports:
            - "6379:6379"
        command: redis-server --appendonly yes --requirepass ""
        volumes:
            - redis:/data

    redis-cache:
        image: redis:alpine
        container_name: redis-messages-cache
        ports:
            - "6380:6379"
        command: redis-server --appendonly yes --requirepass ""
        volumes:
            - redis-cache:/data

    postgres:
        image: postgres:17-alpine
        container_name: postgres-sql-db
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: password
            POSTGRES_DB: strikes-community
        ports:
            - "5432:5432"
        volumes:
            - postgres:/var/lib/postgresql/data

    # app:
    #     image: oven/bun:1
    #     container_name: strikes-app
    #     restart: always
    #     working_dir: /app
    #     volumes:
    #         - .:/app
    #     ports:
    #         - "3000:3000"
    #         - "3001:3001"
    #     env_file: .env
    #     environment:
    #         # Overriding .env values
    #         DATABASE_URL: "postgresql://postgres:password@postgres:5432/strikes-community"
    #         REDIS_URL: "redis://redis:6379"
    #         REDIS_CACHE_URL: "redis://redis-cache:6379"
    #         KAFKA_BROKER: "kafka:29092"
    #         KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
    #         SOCKET_PORT: 3001
    #         NEXT_PUBLIC_SOCKET_URL: "http://localhost:3001"
    #         NEXT_PUBLIC_SITE_URL: "http://localhost:3000"
    #     command: sh -c "bun install && bun x turbo db:generate && bun run dev"
    #     depends_on:
    #         - postgres
    #         - redis
    #         - redis-cache
    #         - kafka

volumes:
    postgres:
    redis:
    redis-cache:
    kafka:
    zookeeper-data:
    zookeeper-log:
